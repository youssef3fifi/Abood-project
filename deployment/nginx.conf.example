# Nginx Configuration Example for TravelExplorer
# This is a reference configuration file

server {
    # Listen on port 80 for HTTP
    listen 80;
    listen [::]:80;
    
    # Replace with your domain name or EC2 public IP
    server_name your-ec2-public-ip.com;
    
    # Root directory for frontend files
    root /var/www/travelexplorer;
    index index.html;
    
    # Main location block
    location / {
        # Try to serve the file directly, otherwise serve index.html (for SPA routing)
        try_files $uri $uri/ /index.html;
    }
    
    # Proxy API requests to backend
    # Uncomment this if you want Nginx to proxy API calls
    # location /api/ {
    #     proxy_pass http://localhost:3000/api/;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection 'upgrade';
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_cache_bypass $http_upgrade;
    # }
    
    # Cache static assets for better performance
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # Gzip compression for text-based files
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml+rss;
    gzip_comp_level 6;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Logs
    access_log /var/log/nginx/travelexplorer-access.log;
    error_log /var/log/nginx/travelexplorer-error.log;
}

# HTTPS configuration (uncomment and configure after obtaining SSL certificate)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     
#     server_name your-domain.com;
#     
#     # SSL certificate paths (use Let's Encrypt or AWS Certificate Manager)
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # Same configuration as HTTP above
#     root /var/www/travelexplorer;
#     index index.html;
#     
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#     
#     # ... (copy other location blocks from above)
# }

# Redirect HTTP to HTTPS (uncomment after SSL setup)
# server {
#     listen 80;
#     listen [::]:80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
